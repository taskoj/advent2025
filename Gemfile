source "https://rubygems.org"

# Use the GitHub Pages gem to match Pages environment and dependencies
gem "github-pages", group: :jekyll_plugins

# Optional: remove warning about Faraday retry middleware
gem "faraday-retry"
